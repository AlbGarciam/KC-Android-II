<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">?attr/colorPrimary</item>
    </style>

    <style name="ToolbarPopup" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="TaskCard" parent="CardView.Light">
        <item name="cardCornerRadius">@dimen/card_radius</item>
        <item name="android:layout_width">match_parent</item>
        <item name="cardElevation">6dp</item>
        <item name="android:layout_height">@dimen/card_height</item>
    </style>

    <style name="MainCTA" parent="TextAppearance.AppCompat.Button">
        <item name="android:background">?colorAccent</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="TaskCard.Content.TextView" parent="CardView.Light">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="TaskCard.Content.Date" parent="CardView.Light">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/darker_gray</item>
    </style>

    <style name="InputText" parent="Widget.AppCompat.EditText">
        <item name="android:backgroundTint">@android:color/transparent</item>
        <item name="android:textSize">16sp</item>
        <item name="android:inputType">textMultiLine</item>
        <item name="android:descendantFocusability">beforeDescendants</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:maxLines">2</item>
        <item name="android:ellipsize">end</item>
        <item name="android:textColor">@drawable/text_color_state_selector</item>
    </style>

    <style name="CustomSwitch" parent="TextAppearance.AppCompat.Widget.Switch">
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:switchPadding">8dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
